# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/stats-cards.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  stats-cards-job:
    environment:
      name: production
    name: Generates stats cards
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@v4
    - name: Generate top languages card
      uses: readme-tools/github-readme-stats-action@v1
      with:
        card: top-langs
        options: username=${{ github.repository_owner }}&langs_count=14&theme=transparent&hide_progress=true&show_icons=true&hide_border=true
        path: top-langs.svg
        token: ${{ secrets.GITHUB_TOKEN }}
    - name: Commit stats cards
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: Update stats cards - [Skip GitHub Action]
        commit_user_email: github-actions@users.noreply.github.com
        commit_user_name: github-actions
        file_pattern: '*.svg'
    timeout-minutes: 30
name: Stats Cards
'on':
  pull_request:
    branches:
    - master
  push:
    branches:
    - master
  schedule:
  - cron: 30 * * * *
  workflow_dispatch: {}
